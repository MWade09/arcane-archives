[build]
  command   = "npm run build"
  publish   = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"

# Set NEXT_PUBLIC_SITE_URL via the Netlify dashboard (Site settings → Environment variables)
# so the signout and other absolute-URL helpers resolve correctly in production.
